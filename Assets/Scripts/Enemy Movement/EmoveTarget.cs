using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EmoveTarget : IEMovement
{
   /*float _speed;
   Transform _Tf;
   transform _target;
   public TarguetMovement(Transform transform,Transform target, float speed = 2){
       _TF = Transform;
       _speed= speed;
       _target = target;

   }
   */
   public void Emovement(){
      // _Tf.position += (_target.position - _Tf.position). normalized * _speed * Time.deltaTime;
   }
   
}
